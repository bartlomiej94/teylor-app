import styles from "./NoExchange.module.scss";

const NoExchange = () => {
  return (
    <div className={styles.container}>
      <h2>Choose amount and currency pair.</h2>
    </div>
  );
};

export default NoExchange;
